import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    fontSize: 32,
    textAlign: 'center',
    padding: 10,
  },
  large: {
    fontSize: 32,
    textAlign: 'center',
    color: 'red',
  },
  normal: {
    fontSize: 22,
    textAlign: 'center',
    color: 'black',
    paddingBottom: 10,
  },
});
